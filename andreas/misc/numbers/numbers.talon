# Numbers only: numb fifty five -> 55
<user.number_prefix>:   "{number_prefix}"

# Floating point numbers only: numb fifty five point two -> 55.2
<user.number_prefix> point <user.number_string>:
    "{number_prefix}.{number_string}"